import React, { useEffect } from 'react';

const ComponentsPage = () => {
  useEffect(() => {
    // You can resize again the widget here | window.ZOHO.CRM.UI.Resize({ height: 300, width: 300 });
  }, []);
  return <div>Components</div>;
};

export default ComponentsPage;
